module.exports = {
  // supporting image formats, default will be applied if empty   
  formats: [],
  // files and directory to ignore, default will be applied if empty
  ignore: [],
  // format sizes generall
  // size: 100,
  // specific
  size: {
    png: 100,
    svg: 20
  }
};
